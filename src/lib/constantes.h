#ifndef CONSTANTES_H
#define CONSTANTES_H

namespace core
{
  const int tileSize = 32;
  const int tileHalfSize = tileSize/2;

  const int unitSize = tileSize;
  const int unitHalfSize = unitSize/2;

  const float tileSizef = static_cast<float>(tileSize);
}

#endif // CONSTANTES_H
